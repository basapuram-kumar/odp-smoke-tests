# Copy to ambari.env (or ambari.config) and set real values (both are gitignored).
#   cp configs/ambari.env.example configs/ambari.env
#
# Smoke scripts auto-load AMBARI_BASE_URL / AMBARI_USER / AMBARI_PASSWORD
# from configs/ambari.env or configs/ambari.config — no need to export them.

AMBARI_BASE_URL=http://10.101.11.138:8080
AMBARI_USER=admin
AMBARI_PASSWORD=admin
